{
	"name": "superdev",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "next dev",
		"build": "next build && next export -o build && mv build/_next build/next && cd build && grep -rli '_next' * | xargs -I@ sed -i '' 's/_next/next/g' @ && cd .. && cp -R css fonts icons js libs manifest.json README.md build &&clear",
		"start": "next start",
		"lint": "next lint"
	},
	"dependencies": {
		"next": "12.2.5",
		"react": "18.2.0",
		"react-dom": "18.2.0"
	},
	"devDependencies": {
		"autoprefixer": "^10.4.8",
		"eslint": "8.23.0",
		"eslint-config-next": "12.2.5",
		"postcss": "^8.4.16",
		"tailwindcss": "^3.1.8"
	}
}
